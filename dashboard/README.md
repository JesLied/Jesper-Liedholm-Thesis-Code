# CMU Simulation Dashboard

Run from the repository root:

```bash
streamlit run dashboard/app.py
```

The dashboard reads only the `Output_Country` sheet from `v4-simulation.xlsx` and uses `Data/europe.geo.json` for the Plotly maps.
